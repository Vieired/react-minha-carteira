export interface BudgetItem{
    description: string;
    amount: number;
    type: string;
    frequency: string;
    date: string;
}

export interface GeneralSearch {
    generalSearch: string
}